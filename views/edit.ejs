<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Tasks</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-zinc-800 text-white">
    <div class="w-full h-screen p-10">
      <a href="/" class="text-blue-300 font-semibold">Back to Home</a>
      <h1 class="text-4xl font-bold mb-6 mt-5">Edit Tasks</h1>
  
      <form method="post" action="/edit/<%= task._id %>" class="flex flex-col space-y-4 max-w-md">
        <input type="text" name="taskTitle" value="<%= task.title %>" class="p-2 rounded text-black" />
        <input type="text" name="taskDesc" value="<%= task.description %>" class="p-2 rounded text-black" />
        <button class="bg-yellow-500 text-white font-bold p-2 rounded">Update Tasks</button>
      </form>
    </div>
  </body>
</html>
