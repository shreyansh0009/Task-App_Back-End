<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-800 text-white">
    <div class="w-full h-screen p-10">
      <a href="/" class="font-semibold">Go Back to Home</a>

      <h2 class="text-3xl text-blue-300 font-semibold mt-10"><%= fileName%></h2>

      <form action="/edit/<%= fileName %>" method="post" class="mt-5">
        <h2 class="text-2xl">
          Previous Name:
          <span class="font-bold text-violet-400"><%= fileName %></span>
        </h2>

        <input
          type="text"
          name="editTitle"
          id="editTitle"
          placeholder="Enter new Title"
          class="p-2 rounded text-black"
        />

        <button
          type="submit"
          class="bg-yellow-500 text-white font-semibold rounded-md p-2 mt-5 ml-1"
        >
          Edit Task
        </button>
      </form>
    </div>
  </body>
</html>
